import config from '../mix/constants'
import defaultTheme from './default/index.style'

const themes = {
    defaultTheme: defaultTheme
}

// export default themes[config.THEME]
export default themes.defaultTheme